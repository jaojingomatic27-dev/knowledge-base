# 项目日志 — news

## [2026-06-04 23:35] 简报风格 + 中英双语 + 公司名高亮

- **输入命令**: "格式很好保留 不用把新闻列出来 你看邮新闻 总结然后只给我看简报" + "新闻翻译成中文 英文原文跟在后面 每个新闻提到的公司 公司名字高亮"
- **PROJECT_INDEX 变更**: 无
- **关键发现**:
  1. 邮件改为简报风格：不再逐条罗列新闻，改为每个组合一张卡片（信号+标签+最多3条关键标题）
  2. 中英双语标题：中文翻译在前（深色粗体），英文原文在后（灰色小字），用 `deep-translator` (Google Translate) 实现
  3. 公司名高亮：中英文均支持（如 博通→红色、英伟达→绿色、特斯拉→红色），用 `<span>` 着色
  4. 翻译缓存持久化到 `data/translation_cache.json`，首次 246 条全翻译，后续仅翻译新增标题
  5. 限速策略：每 5 条暂停 0.5 秒，避免触发 Google 反爬
  6. 246 条标题翻译全部成功，新增翻译 246 条入缓存
- **生成/修改的文件**:
  | 文件 | 说明 |
  |------|------|
  | `code/daily_news_scanner.py` | 新增翻译+高亮+简报重构 |
  | `data/translation_cache.json` | 246 条英→中翻译缓存 |
  | `.gitignore` | 排除 data/*.json data/*.html |

## [2026-06-04 23:15] SMTP 认证打通 + 首封邮件发送成功

- **输入命令**: "Google 应用专用密码 lpks qytc xmqd hikj"
- **PROJECT_INDEX 变更**: 无
- **关键发现**:
  1. Google 要求应用专用密码（16 位），普通密码在 SMTP/IMAP 上均被拒绝
  2. 新应用密码在 SMTP (587 STARTTLS) 和 IMAP (993 SSL) 上均验证通过
  3. 首封正式报告邮件成功发送到 jaojingomatic27@googlemail.com
  4. 同步更新 `C:\AI\cc\account.txt`（全局密码文件，mail 项目共用）
  5. 最新扫描：244 条新闻，强烈利多 14 / 利多 10 / 中性 211 / 利空 4 / 强烈利空 5
- **生成/修改的文件**:
  | 文件 | 说明 |
  |------|------|
  | `account.txt` | 更新为 16 位应用专用密码 |
  | `C:\AI\cc\account.txt` | 同上（全局共用） |

## [2026-06-04 23:00] 每日盘前新闻扫描系统初始化


- **输入命令**: "每天美股开盘前一个小时 搜索全网过去24小时关于这些公司的会影响股价的新闻，给出评价 是利多还是利空"
- **PROJECT_INDEX 变更**: 新建项目，创建完整文件结构
- **关键发现**:
  1. 覆盖 8 只标的（铁三角 NVDA/MSFT/ORCL + 窜天猴 PLTR/SMCI/TSLA + DCA SPY/AVGO），对应 3 个投资组合
  2. 双数据源：yfinance `.news` 每标的 10 条 + Google News RSS 每标的 10-74 条，去重后约 250 条/天
  3. 自定义金融情感词典：50+ 利多/利空正则模式，含修饰词加权（amplifier ×1.3 / diminisher ×0.7）
  4. 首测结果：246 条新闻，强烈利多 14 / 利多 9 / 中性 213 / 利空 5 / 强烈利空 5
  5. AVGO 财报不及预期 + 降级被正确识别为强烈利空(-6.0)；ORCL AI 收入激增+积压订单被正确识别为强烈利多(+8.0)
  6. 邮件通过 Gmail SMTP 发送 HTML 报告，深色主题，按投资组合分组，高亮高影响新闻
  7. Windows 定时任务：每日 20:30 CST（美东 08:30 EDT）执行
- **生成/修改的文件**:
  | 文件 | 说明 |
  |------|------|
  | `CLAUDE.md` | 项目专用规则 |
  | `PROJECT_INDEX.md` | 项目文件索引 |
  | `PROJECT_LOG.md` | 本日志 |
  | `code/daily_news_scanner.py` | 核心脚本：搜索+情感分析+邮件 |
  | `code/setup_scheduled_task.ps1` | Windows 定时任务注册脚本 |
  | `data/news_20260604.json` | 首测新闻数据快照 (246 条) |
